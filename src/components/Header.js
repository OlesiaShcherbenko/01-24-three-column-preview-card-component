import React from "react";
import './Header.css';

/*
const headers = ['Sedans', 'SUVs', 'Luxury'];

const headersList = headers.map((header, i) =>
    <h1 key={'header_' + i}>{header}</h1>
);
*/

function Header() {
    return <h1>Sedans</h1>
}

export default Header;